4. Closing the AI report or the app during generation caused Tk callbacks from the worker thread to target destroyed widgets.
   - Decision: workers only write to a queue; the Tk main thread polls and owns all widget updates.
5. Unexpected endpoint or response errors could leave the report UI permanently in `생성 중…`.
   - Decision: wrap malformed URLs/JSON/content and restore controls for every worker exception.
6. Rapid opacity preview scheduled unbounded delayed alpha callbacks.
   - Decision: debounce delayed alpha reapplication to at most two jobs.

## Verification

- 56 automated tests pass.
- Repeated month changes, 80-task day-cell entry, repeated edits, and 100 opacity preview changes complete without Tk callback errors.
- SQLite `PRAGMA integrity_check` returns `ok` after multi-day task creation and carry-over.
- Wheel packaging includes the 2020–2050 holiday fallback data.

## Remaining platform risks

These require a real Windows session and are not claimed as verified by the Linux audit environment:

- WorkerW variants across Windows 10/11 and Explorer updates.
- Mixed-DPI monitor coordinate behavior.
- Mouse hit-testing through the transparent-color foreground window, especially blank areas inside task rows and toolbar controls.
- Detach failure caused by an already-destroyed Explorer parent.
__DAYMARK_CHANGED_003__
mkdir -p 'docs'
cat > 'docs/PRODUCT_SPEC.md' <<'__DAYMARK_CHANGED_004__'
# Product Specification

## 1. Problem

업무를 캘린더, 할 일 앱, 메모, 업무보고 문서에 반복 입력하면 기록 비용이 커진다. 별도 앱 창을 찾아 열어야 하는 마찰도 매일 누적된다.

## 2. Product Goal

바탕화면 달력의 날짜 셀에 남긴 체크리스트를 업무 원장으로 사용하고, 선택 기간의 기록을 제출 가능한 보고서로 변환한다.

## 3. Target User

- 개인 업무를 빠르게 남기는 사무·연구·개발 직군
- 프로젝트 관리 도구까지는 필요하지 않은 사용자
- 주간 또는 월간 업무보고를 반복 작성하는 사용자
- Windows 바탕화면에서 일정과 할 일을 항상 확인하고 싶은 사용자

## 4. MVP Functional Requirements

### FR-01 월간 달력

- 월요일 시작 7열과 고정 6주·42개 날짜 셀을 유지한다.
- 이전·다음 달 날짜는 흐리게 표시한다.
- 이전 달, 다음 달, 오늘 이동은 창의 가로 중앙에 둔다.
- 날짜 셀은 테두리나 카드 배경 없이 정렬과 여백으로 구분한다.

### FR-02 날짜별 업무 입력

- 빈 입력 행은 평소 배경과 같아 입력 상자처럼 보이지 않는다.
- 날짜 셀의 빈 영역을 클릭하면 해당 날짜 입력 행에 포커스를 둔다.
- `Enter`는 저장 후 다음 빈 행에 포커스를 둔다.
- 업무는 한 줄 텍스트다.

### FR-03 완료 체크

- 저장된 업무 왼쪽에 Canvas 기반 원형 체크 컨트롤을 표시한다.
- 체크 변경은 즉시 저장한다.
- 완료 업무는 흐린 글자로 표시한다.

### FR-04 편집과 삭제

- 업무 텍스트를 직접 수정한다.
- 포커스를 잃거나 `Enter`를 누르면 저장한다.
- 기존 업무를 빈 문자열로 만들면 삭제한다.

### FR-05 미완료 오늘로 이동

- 오늘 이전 모든 날짜의 미완료 업무를 오늘 마지막 순서로 복사한다.
- 원래 날짜의 업무는 완료 여부와 관계없이 삭제하지 않는다.
- 같은 원본 업무는 같은 대상 날짜에 한 번만 복사한다.
- 이전에 복사된 사본이 다시 후보가 되더라도 원본 계보별 하나만 복사한다.
- 계보의 가장 최근 사본이 완료되면 오래된 원본이 미완료로 남아 있어도 다시 복사하지 않는다.

### FR-06 주말·대한민국 공휴일

- 토요일 날짜는 낮은 채도의 파란색, 일요일·대한민국 공휴일 날짜는 낮은 채도의 빨간색으로 표시한다.
- 토·일 요일 헤더는 날짜보다 한 단계 흐리게 표시한다.
- 현재 월 밖 날짜와 업무는 같은 규칙을 더 낮은 명암으로 적용한다.
- `python-holidays`의 `KR` 공휴일 달력을 우선 사용한다.
- 라이브러리를 불러올 수 없는 경우 2020~2050년 내장 데이터를 사용한다.

### FR-07 보고서

- 일간, 주간, 월간 기간을 지원한다.
- 로컬 미리보기는 오프라인으로 동작한다.
- 사용자가 명시적으로 누를 때만 기록을 LLM API에 보낸다.
- 결과를 편집하고 클립보드에 복사할 수 있다.

### FR-08 로컬 저장

- SQLite를 사용한다.
- 계정과 서버 없이 동작한다.
- 앱 재시작 후 기록과 체크 상태를 복원한다.

### FR-09 Windows 바탕화면 통합

- Windows 첫 실행은 바탕화면 모드를 기본값으로 한다.
- Explorer의 `SHELLDLL_DefView`가 있는 창을 기준으로 뒤쪽 `WorkerW`를 찾는다.
- WorkerW가 없고 Progman이 아이콘 호스트이면 Progman으로 폴백한다.
- 연결된 모니터 목록을 읽고 사용자가 선택한 모니터 한 대의 좌표와 해상도에만 Daymark를 배치한다.
- WorkerW가 가상 데스크톱 전체를 덮더라도 선택 모니터 좌표를 부모 기준 상대 좌표로 변환한다.
- 바탕화면 연결과 유지 시 `WS_EX_LAYERED` 및 alpha 값을 다시 적용한다.
- Explorer 재시작으로 부모 핸들이 사라지면 주기적으로 재탐색한다.
- 연결 실패 시 원래 부모와 창 스타일을 복원하고 일반 창으로 동작한다.
- `···` 메뉴, `Ctrl+Shift+D`, `--window`, `--desktop`으로 모드를 전환한다.
- 설정에서 표시 모니터와 투명도를 변경하고 즉시 적용한다.
- 일반 창에서 바탕화면 모드로 전환하면 현재 창이 가장 많이 걸쳐 있는 모니터를 자동 선택한다.

## 5. Non-functional Requirements

- 공휴일 라이브러리를 설치하지 않은 ZIP 직접 실행에서도 내장 데이터로 달력이 동작해야 한다.
- 네트워크 없이 기록과 체크 기능이 모두 동작해야 한다.
- API 키를 영구 저장하지 않는다.
- 배경 기본 불투명도는 0.94, 최소값은 0.78이며 창/바탕화면 모드에서 같은 설정값을 사용한다.
- Windows에서는 전경 텍스트와 체크를 불투명하게 유지한다.
- 바탕화면 통합 실패가 데이터베이스 트랜잭션에 영향을 주지 않는다.
- WorkerW 코드와 업무 도메인 코드를 분리한다.

## 6. Out of Scope

로그인, 클라우드 동기화, 협업, 태그, 프로젝트, 우선순위, 반복 일정, 알림, 모바일 앱, 외부 캘린더 연동, 채팅형 AI는 MVP 범위 밖이다.

## 7. Acceptance Scenario

1. Windows에서 앱을 실행한다.
2. Daymark가 WorkerW 또는 Progman에 연결되어 선택한 모니터 한 대에만 표시된다.
3. 2026년 7월 16일에 `코터 비전 회의`를 입력하고 `Enter`를 누른다.
4. 다음 행에 `Y-profile 데이터 정리`를 입력한다.
5. 첫 업무를 완료 체크한다.
6. 주간 로컬 미리보기에서 완료와 미완료가 구분된다.
7. `창 모드` 또는 `Ctrl+Shift+D`로 일반 창으로 복귀한다.
8. `미완료 오늘로 이동`을 실행하면 과거 미완료가 오늘에 복사되고 원본은 남는다.
9. 토요일과 일요일·대한민국 공휴일은 업무보다 약한 채도의 주말색으로 보인다.
10. Explorer 레이어 탐색 실패 시에도 기록은 그대로 남고 일반 창이 열린다.

### FR-09 시각적 선택과 오버플로

- 오늘 날짜와 선택 날짜를 서로 다른 상태로 표시한다.
- 선택 날짜는 낮은 대비의 배경만 사용하며 테두리나 그림자를 추가하지 않는다.
- 날짜별 업무가 셀 높이를 넘으면 해당 날짜 셀에서만 스크롤한다.
- 상단 우측에는 `AI 요약`과 `···`만 기본 노출한다.
__DAYMARK_CHANGED_004__
mkdir -p 'docs'
cat > 'docs/ROADMAP.md' <<'__DAYMARK_CHANGED_005__'
# Roadmap

로드맵 항목은 승인된 기능이 아니라 후보 목록이다. 구현 전 `AGENTS.md`의 Scope Guard를 확인한다.

## v0.1 Complete

- 월간 달력
- 인라인 체크리스트
- SQLite 저장
- 미완료 업무를 오늘로 이동
- 일간·주간·월간 요약
- OpenAI 호환 보고서

## v0.2 Complete

- 중앙 내비게이션
- 무테두리 날짜 레이아웃
- 반투명 평면 디자인
- 빈 입력칸과 체크박스 숨김

## v0.3 Implemented

- Windows WorkerW/Progman 바탕화면 연결
- 일반 창 전환과 설정 저장
- Explorer 재시작 재연결
- 연결 실패 롤백
- `--window` / `--desktop` 복구 옵션

## v0.4 Complete

- 선택 모니터 한 대에만 바탕화면 배치
- 설정에서 모니터 선택
- 설정에서 투명도 조절 및 즉시 적용
- WorkerW attach/maintain 시 layered alpha 재적용

## v0.5 Complete

- 오늘 이전 모든 미완료 업무를 원본 보존 방식으로 오늘에 복사
- 복사 계보와 같은 날 중복 방지
- 일반 창이 있는 모니터로 바탕화면 모드 자동 전환
- 토요일 파란색, 일요일·대한민국 공휴일 빨간색
- `python-holidays` 우선 사용과 2020~2050년 내장 폴백

## v0.6 Complete

- Apple 계열 팔레트와 공통 폰트 토큰
- 기본/최소 투명도 상향 및 글자 불투명 유지
- 오늘 원형 표시와 선택 날짜 배경
- 상단 `AI 요약 ···` 구조와 관리 기능 메뉴 이동
- 원형 체크 컨트롤과 날짜별 오버플로 스크롤
- 설정·AI 요약 창 스타일 통일
- 논리 단위 `Why / Decision` 커밋 기록

## v0.7 Complete

- 고정 6주·42칸과 현재 월 밖 날짜·업무의 약한 명암
- 낮은 채도의 주말·공휴일 색상과 흐린 요일 헤더
- 비모달 평면 더보기 팝업과 비차단 미완료 복사 토스트
- 24px 원형 체크 클릭 영역과 키보드 포커스 표시
- 커스텀 평면 투명도 슬라이더 및 설정·AI 요약 계층 정리

## Next Candidates

- Windows 10/11 실기기 호환성 매트릭스
- Windows 단일 실행 파일 패키징
- Windows 시작 프로그램 등록
- 날짜 셀 내부 스크롤 또는 접기
- 보고서 이력 조회
- Markdown 내보내기

## Explicitly Deferred

로그인, 클라우드 동기화, 협업, 태그·프로젝트, 반복 일정, 외부 캘린더 연동은 제품 정체성을 다시 검토하기 전까지 보류한다.


## v0.6.1 Audit Complete

- 단일 날짜 셀의 빈 입력 행 중복 누적 제거
- 전경/배경 WorkerW 부분 실패 시 원자적 폴백
- AI 생성 스레드와 Tk UI 생명주기 분리
- 잘못된 LLM URL·JSON·content 응답 복구
- 투명도 미리보기 지연 콜백 디바운스
- 56개 회귀 테스트 및 엄격 감사 기록
__DAYMARK_CHANGED_005__
mkdir -p 'docs'
cat > 'docs/TEST_PLAN.md' <<'__DAYMARK_CHANGED_006__'
# Test Plan

## Unit

- 월간 42일과 월 이동 말일 보정
- SQLite CRUD, 순서, 체크, 삭제, 과거 전체 미완료 원본 보존 복사와 중복 방지
- 보고서 사실성과 LLM 응답 파싱
- WorkerW 연결 시 부모와 스타일 변경
- 분리 시 원래 부모·스타일 복원
- Explorer 부모 교체 후 재연결
- 연결 중 일부 실패 시 롤백
- 비 Windows 폴백의 안전한 no-op
- `--window`와 `--desktop` 상호 배타성
- 현재 창 모니터 자동 탐색 후 바탕화면 대상 선택
- 대한민국 공휴일과 대체공휴일 조회, 2050년 내장 폴백

## GUI Smoke

- 앱 생성과 종료
- 날짜 셀 42개
- 중앙 내비게이션
- 무테두리·무흰칸 시각 계약
- 저장 전 체크 숨김, 저장 후 표시
- 불투명도 범위
- 가짜 DesktopHost로 모드 전환과 설정 저장
- 낮은 채도의 토요일·일요일·공휴일 색상과 더 흐린 요일 헤더

## Commands

```bash
PYTHONPATH=src python -m compileall src
PYTHONPATH=src python -m unittest discover -s tests -v
PYTHONPATH=src xvfb-run -a python scripts/check.py
```

## CI Matrix
